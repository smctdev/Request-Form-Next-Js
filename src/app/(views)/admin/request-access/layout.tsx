import { ReactNode } from "react";

export const metadata = {
  title: "Request Access of Employees",
  description:
    "SMCT Group of Companies Request Form request access of employees page.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

import { ReactNode } from "react";

export const metadata = {
  title: "Positions",
  description: "SMCT Group of Companies Request Form positions page.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

import { ReactNode } from "react";

export const metadata = {
  title: "Branch Heads",
  description: "SMCT Group of Companies Request Form branch heads page.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

import { ReactNode } from "react";

export const metadata = {
  title: "Feedbacks",
  description: "SMCT Group of Companies Request Form feedbacks page.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

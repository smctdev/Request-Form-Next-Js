import { ReactNode } from "react";

export const metadata = {
  title: "Area Managers",
  description: "SMCT Group of Companies Request Form area managers page.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

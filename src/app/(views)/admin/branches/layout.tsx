import { ReactNode } from "react";

export const metadata = {
  title: "Branches",
  description: "SMCT Group of Companies Request Form branches page.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

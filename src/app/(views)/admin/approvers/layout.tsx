import { ReactNode } from "react";

export const metadata = {
  title: "Admin Approvers",
  description: "SMCT Group of Companies Request Form admin approvers page.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

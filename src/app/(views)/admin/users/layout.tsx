import { ReactNode } from "react";

export const metadata = {
  title: "Users | Employees",
  description: "SMCT Group of Companies Request Form users/employees page.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

import { ReactNode } from "react";

export const metadata = {
  title: "AVP Staffs",
  description: "SMCT Group of Companies Request Form AVP Staffs page.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

import { ReactNode } from "react";

export const metadata = {
  title: "Registration",
  description: "SMCT Group of Companies Request Form registration page.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

import { ReactNode } from "react";

export const metadata = {
  title: "Login",
  description: "SMCT Group of Companies Request Form login page.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

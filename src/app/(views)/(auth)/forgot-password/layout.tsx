import { ReactNode } from "react";

export const metadata = {
  title: "Forgot Password",
  description: "SMCT Group of Companies Request Form forgot password page.",
};

export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
